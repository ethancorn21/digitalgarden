[[Programming]]
#baby

Static methods are methods that can only be called by the class in which they reside.
For example, if i was in class2, but wanted to call the method i created in class1 and it was a static method, I would call it like this: class1.staticmethod1(valueToBePassedToMethod).